# dots-and-boxes-mcts
Dots and boxes using monte carlo tree search


To run MCTS(mcts.py) against another bot greedy (my_bot), greedy after playing out the whole game(boba), random bot(random_bot) use
the following command "python3 simulator.py mcts greedy"

For a graphical interface use "python3 interactive.py mcts greedy"